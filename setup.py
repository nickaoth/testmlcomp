from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A model training ML to predict what champion will be select to pick based a df with matches that champion was better in acording to the situations.',
    author='Nicolas Thomazini',
    license='MIT',
)
